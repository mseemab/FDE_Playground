// Scaffolder entry point: `pnpm new-project <slug>`. Implemented in Phase 3 (see docs/exec-plans).
console.error("new-project is not implemented yet (lands in Phase 3 of the factory setup).");
process.exit(1);
